# Utility aliases
alias lsa="ls -lA"
alias cd..="cd .."

# Helpful bits
alias myip="curl http://ipecho.net/plain; echo"

# Git aliases
alias gpush="git push origin master"
alias ga="git add ."
gc() {
    git commit -m "$1"
}